// C++ STL

// Multimap

#include <bits/stdc++.h>
using namespace std;

void explainMultimap() {

	  // Multimap is similiar to map, only it can store duplicate keys 
    // It is also sorted

}

int main() {
	
	explainMultimap();
	return 0;
}
